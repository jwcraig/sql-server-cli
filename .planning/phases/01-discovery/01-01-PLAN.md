---
phase: 01-discovery
plan: 01
type: execute
---

<objective>
Analyze current describe command patterns and SQL Server catalog views to document the implementation approach for parameter inspection.

Purpose: Establish clear understanding of existing code structure and SQL Server metadata availability before implementation.
Output: DISCOVERY.md documenting findings and implementation approach.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/commands/describe.rs
@src/db/queries.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document describe.rs flow and output patterns</name>
  <files>.planning/phases/01-discovery/DISCOVERY.md</files>
  <action>
    Analyze describe.rs and document in DISCOVERY.md:

    1. **Current procedure handling** (describe_procedure function):
       - Query structure at lines 614-634
       - Fields already queried: param_name, data_type, max_length, precision, scale, is_output
       - What's displayed vs queried (precision/scale queried but not shown!)

    2. **Current function handling** (describe_function function):
       - Separate query at lines 724-743
       - Only shows name and dataType (missing more than procs)

    3. **Output patterns**:
       - JSON: json! macro with structured payload
       - Text: table::render_result_set_table()
       - Helper functions: format_params_result_set(), format_fn_params_result_set()

    4. **Data structures**:
       - No ParameterInfo struct exists (inline query result handling)
       - ResultSet used for tabular data

    5. **Integration points for enhancement**:
       - describe_procedure() at line 603
       - describe_function() at line 689
       - format_params_result_set() at line 1511
       - format_fn_params_result_set() at line 1554
  </action>
  <verify>DISCOVERY.md contains "Current Implementation Analysis" section with specific line references</verify>
  <done>Describe command flow documented with line numbers and specific findings</done>
</task>

<task type="auto">
  <name>Task 2: Research sys.parameters schema for complete metadata</name>
  <files>.planning/phases/01-discovery/DISCOVERY.md</files>
  <action>
    Research and document SQL Server catalog views:

    1. **sys.parameters columns** (document all relevant columns):
       - object_id, name, parameter_id
       - user_type_id, system_type_id (for type resolution)
       - max_length, precision, scale
       - is_output (current) - maps to OUTPUT keyword
       - has_default_value (boolean flag)
       - is_nullable
       - is_readonly (for table-valued parameters)

    2. **sys.types join** for type name resolution:
       - TYPE_NAME(user_type_id) already used

    3. **Default value extraction challenge**:
       - sys.parameters.has_default_value only indicates presence, not value
       - Default values embedded in object definition (OBJECT_DEFINITION)
       - Options: parse from definition or just show "has default: yes/no"

    4. **Parameter direction mapping**:
       - is_output=0 → IN
       - is_output=1 → OUTPUT (SQL Server uses OUTPUT, not OUT/INOUT)

    5. **Recommended query structure** for enhanced parameter fetch:
       ```sql
       SELECT
           p.name,
           TYPE_NAME(p.user_type_id) AS data_type,
           p.max_length,
           p.precision,
           p.scale,
           p.is_output,
           p.has_default_value,
           p.is_nullable
       FROM sys.parameters p
       ...
       ```

    6. **Key decisions to document**:
       - Show direction as "IN"/"OUTPUT" (matching SQL Server terminology)
       - Show has_default_value as boolean (extracting actual value is complex)
       - Format type with length/precision/scale inline (e.g., varchar(50), decimal(10,2))
  </action>
  <verify>DISCOVERY.md contains "SQL Server Catalog Analysis" section with query examples</verify>
  <done>Catalog schema documented with recommended query structure and key decisions</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] DISCOVERY.md created in .planning/phases/01-discovery/
- [ ] Current implementation analysis complete with line references
- [ ] SQL Server catalog columns documented
- [ ] Recommended query structure provided
- [ ] Key decisions documented (direction terminology, default handling)
</verification>

<success_criteria>

- DISCOVERY.md exists with complete analysis
- All integration points identified
- Recommended approach for enhanced parameter metadata clear
- No ambiguity about what to build in subsequent phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Analyze Describe Patterns Summary

**[One-liner describing key findings]**

## Accomplishments

- [Key finding 1]
- [Key finding 2]

## Files Created/Modified

- `.planning/phases/01-discovery/DISCOVERY.md` - Analysis document

## Decisions Made

- [Key decisions about approach]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Ready for Phase 2: Database Query implementation
</output>
