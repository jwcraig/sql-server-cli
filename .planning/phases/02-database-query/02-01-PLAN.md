---
phase: 02-database-query
plan: 01
type: execute
---

<objective>
Add `has_default_value` and `is_nullable` columns to parameter queries for stored procedures and functions.

Purpose: Enable downstream phases to display complete parameter metadata including defaults and nullability.
Output: Updated SQL queries in describe.rs that fetch all required parameter columns.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-discovery/DISCOVERY.md
@src/commands/describe.rs

**Constraining decisions from Phase 1:**
- Direction terminology: "IN"/"OUTPUT" (matches SQL Server syntax)
- Default value: Show boolean flag only (extraction is complex/fragile)
- Type formatting: Inline like `varchar(50)` (uses existing helper)
- Data structure: ParameterInfo struct coming in Phase 3

**Key context from DISCOVERY.md:**
- Procedure query at lines 614-628: Already has precision/scale but not displayed; needs has_default_value, is_nullable
- Function query at lines 724-737: Missing precision, scale, has_default_value, is_nullable
- Query output order matters for downstream consumers (maintain column indices)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add columns to procedure parameter query</name>
  <files>src/commands/describe.rs</files>
  <action>
In the `describe_procedure` function (around lines 614-628), update the SQL query to add `has_default_value` and `is_nullable` columns AFTER the existing columns.

Current query ends with:
```sql
    p.is_output
FROM sys.parameters p
```

Update to:
```sql
    p.is_output,
    p.has_default_value,
    p.is_nullable
FROM sys.parameters p
```

**Critical:** Do NOT change the order of existing columns (name, data_type, max_length, precision, scale, is_output) as downstream code uses row indices. Only APPEND the two new columns.

The full enhanced query should match DISCOVERY.md's recommended structure.
  </action>
  <verify>cargo check -- no compilation errors; the query string is syntactically updated</verify>
  <done>Procedure parameter query includes has_default_value and is_nullable as final columns (indices 6 and 7)</done>
</task>

<task type="auto">
  <name>Task 2: Add columns to function parameter query</name>
  <files>src/commands/describe.rs</files>
  <action>
In the `describe_function` function (around lines 724-737), update the SQL query to add `precision`, `scale`, `has_default_value`, and `is_nullable` columns.

Current query only has:
```sql
    p.name AS param_name,
    TYPE_NAME(p.user_type_id) AS data_type,
    p.max_length,
    p.is_output
```

Update to match the procedure query structure:
```sql
    p.name AS param_name,
    TYPE_NAME(p.user_type_id) AS data_type,
    p.max_length,
    p.precision,
    p.scale,
    p.is_output,
    p.has_default_value,
    p.is_nullable
```

**Critical:** Maintain same column order as procedure query for consistency. This adds 4 columns total (precision, scale, has_default_value, is_nullable).
  </action>
  <verify>cargo check -- no compilation errors; function query matches procedure query column structure</verify>
  <done>Function parameter query has same columns as procedure query (8 total: name, data_type, max_length, precision, scale, is_output, has_default_value, is_nullable)</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `cargo check` passes with no errors
- [ ] `cargo clippy` passes with no warnings
- [ ] Procedure query has 8 columns in correct order
- [ ] Function query has same 8 columns in same order
- [ ] No other code changes made (output handling is Phase 4-8)
</verification>

<success_criteria>

- Both queries updated with all required columns
- Column order consistent between procedure and function queries
- No compilation errors or warnings
- No changes to output handling (those come in later phases)
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-query/02-01-SUMMARY.md`:

# Phase 2 Plan 1: Database Query Summary

**[One-liner describing what changed]**

## Accomplishments

- Updated procedure parameter query
- Updated function parameter query

## Files Created/Modified

- `src/commands/describe.rs` - Added columns to parameter queries

## Decisions Made

[Any decisions or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Ready for Phase 3: Data Model (ParameterInfo struct)
</output>
